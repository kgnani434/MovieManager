import React, { Component } from 'react';
const Pagination = props => {
    const {pageSize, currentPage, items} = props;
    
    return ( <ul>
        <li></li>
    </ul>  );
}
 
export default Pagination;